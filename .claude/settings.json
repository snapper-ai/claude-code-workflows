{
  "permissions": {
    "allow": [
      "npm test",
      "npm run build",
      "npm run lint",
      "npm run format",
      "npm run typecheck",
      "git status",
      "git diff",
      "git log"
    ]
  }
}
